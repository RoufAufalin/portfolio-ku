# Porto Linktree (React)

Minimalist Linktree-style personal page built with React + Vite.

## Local dev

1. Install deps: `npm install`
2. Run: `npm run dev`

## Edit links

Update your profile + links in `src/siteConfig.js`.

